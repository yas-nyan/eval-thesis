ethernets:
    ens192:
        dhcp4: false
        accept-ra: false
        addresses:
         - "{{ ipv4 }}"
         - "{{ ipv6 }}"
        gateway4: "{{ gateway4 }}"
        gateway6: "{{ gateway6 }}"
        nameservers:
            addresses:
{%- for nameserver in nameservers %}
             - "{{ nameserver }}"
{%- endfor %}
            search:
             - sfc.wide.ad.jp
version: 2
